python-dotenv==1.0.1
httpx==0.27.2
python-telegram-bot==22.7
matplotlib==3.9.2
anyio>=4.0.0
pytest>=7.0

# Phase 1+ — structured profile + 3-stage learner
PyYAML>=6.0

# Phase 2 — async SQLite ToDo store
aiosqlite==0.21.0

# Phase 5 — sqlite-vec semantic layer
sqlite-vec>=0.1.9

# Kicktipp client parses HTML with BeautifulSoup (imported at module top, so
# test collection fails without it even though tests mock the network)
beautifulsoup4>=4.12

# Google Calendar client (gcal_client.py imports at module top, even though
# tests monkeypatch the network functions)
google-api-python-client==2.196.0
google-auth-httplib2==0.4.0
google-auth-oauthlib==1.3.1
